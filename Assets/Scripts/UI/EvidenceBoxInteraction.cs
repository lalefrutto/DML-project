using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.EventSystems;
using UnityEngine.UI;

public class EvidenceBoxInteraction : Interactable 
{
    // private Button button;
    protected override string ActionDescription => "Рассмотреть улики (в разработке)";

    void Start()
    {
        // button = GetComponent<Button>();
    }
}